const light = {
  color: "#000000",
  background: "#eeeeee",
  isDark: false,
};

const dark = {
  color: "#ffffff",
  background: "#222222",
  isDark: true,
};

const theme = {
  light,
  dark,
};

export default theme;
